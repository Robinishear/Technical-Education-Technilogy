import StudentsList from '@/features/usersDashboard/Students-add/StudentsList';
import React from 'react';

const page = () => {
    return (
        <div>
            <StudentsList/>
        </div>
    );
};

export default page;