import StudentForm from '@/features/usersDashboard/Students-add/Students';
import React from 'react';

const page = () => {
    return (
        <div>
            <StudentForm/>
        </div>
    );
};

export default page;